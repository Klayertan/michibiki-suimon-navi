# Test-only dependencies for the MAVLink backend.
#   .venv\Scripts\python.exe -m pip install -r backend/requirements-dev.txt
-r requirements.txt

pytest~=8.3.0
# httpx powers fastapi.testclient; websockets gives TestClient real WS support.
httpx~=0.27.0
websockets~=13.1
